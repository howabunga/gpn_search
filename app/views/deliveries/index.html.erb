<div class="page-header">
  <h1>
    Search Product by Delivery Type
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Name</th>
        <th></th>
      </tr>

      <% @deliveries.each do |delivery| %>
      <tr>
        <td><%= delivery.name %></td>
        <td>
          <a href="/deliveries/<%= delivery.id %>" class="btn btn-warning"><i class="fa fa-search"></i> Search</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
